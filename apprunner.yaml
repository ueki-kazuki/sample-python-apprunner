version: 1.0
runtime: python3 
build:
  commands:
    build:        
      - pip install pipenv
      - pipenv install

run:
  command: python server.py
  env:
    - name: FLASK_APP
      value: "server"
    - name: FLASK_ENV
      value: "development"
